# Operand limits for the calculator.
# Backend reads CALC_MIN / CALC_MAX at runtime (via os.Getenv).
# Frontend reads VITE_CALC_MIN / VITE_CALC_MAX at dev-server start / build time.
CALC_MIN=-1000
CALC_MAX=1000
VITE_CALC_MIN=-1000
VITE_CALC_MAX=1000
